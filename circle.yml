machine:
  services:
    - docker
 
dependencies:
  override:
    - docker build -t quay.io/levlaz/braindump:$CIRCLE_TAG .
    - docker tag quay.io/levlaz/braindump:$CIRCLE_TAG quay.io/levlaz/braindump:latest

test:
  override:
    - echo "No Tests"
    # - docker run quay.io/levlaz/braindump python manage.py test

deployment:
  docker:
    tag: /v[0-9]+(\.[0-9]+)*/
    owner: levlaz
    commands:
      - docker login -e="." -u $DOCKER_USER -p $DOCKER_PASS quay.io
      - docker push quay.io/levlaz/braindump
      - scripts/deploy.sh